import 'package:flutter/cupertino.dart';

class DataCards {
  String subject;
  String classId;
  String punchIn;
  String punchOut;
  Color color;

  DataCards({this.punchIn, this.punchOut, this.classId, this.color, this.subject});
}
